package lesson12;

public class ShoesPairs {
  int calc(String origin) {
    int intervals = 0;
    for (int i = 0; i < origin.length(); i++) {
      switch (origin.charAt(i)) {
        case 'L': break;
        case 'R': break;
      }
    }
    return intervals;
  }
}
